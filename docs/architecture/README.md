# Chronobreak architecture documentation

This directory contains durable architecture facts discovered from the actual repository.

The one-time repository audit should replace or extend this placeholder with concise documents covering the architecture that future agents need to understand reliably.

Prefer focused documents such as:
- `overview.md`
- `recorder.md`
- `match-data.md`
- `media-storage.md`
- `frontend-replay.md`

Do not duplicate implementation detail that is obvious from the code. Document boundaries, invariants, lifecycle, durable design decisions, and non-obvious constraints.
